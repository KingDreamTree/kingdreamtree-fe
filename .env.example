# REFIT API v1 base URL. Keep the /api/v1 suffix.
VITE_API_BASE_URL=https://api.refit.live/api/v1

# Leave blank to use the legacy API upload flow. Set only at build time when
# the privacy-preserving pod flow is live (for local testing: http://localhost:8080).
VITE_POD_BASE_URL=
